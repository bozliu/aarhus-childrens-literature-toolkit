# README Source Note

The canonical `README.md` is generated by the shared Python pipeline:

```bash
python -m childlit_toolkit render
```

or through the R wrapper:

```bash
Rscript scripts/render_readme.R
```

This file is intentionally lightweight so the repository still documents an R-facing README entrypoint while keeping one shared generated `README.md` as the public source of truth.
